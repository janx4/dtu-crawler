const { SEMESTER_ID, URL_ALL_COURSES, getSubjectUrl } = require("./urls");
const { ALL_COURSES_FILE_NAME, ALL_SUBJECTS_FILE_NAME } = require("./fileName");

module.exports = {
    urls: { SEMESTER_ID, URL_ALL_COURSES, getSubjectUrl },
    fileName: { ALL_COURSES_FILE_NAME, ALL_SUBJECTS_FILE_NAME },
};
